# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/
frontend/dist/

# Env
.env

# Data (raw + derived, not committed)
*.csv
*.parquet
backend/data/raw/*
!backend/data/raw/.gitkeep

# Fitted models and validation artifacts: regenerated by scripts/train_models.py and
# scripts/validate_risk_levels.py. ~23 MB of joblib plus a 7 MB scored-sample dump, so they
# are rebuilt rather than committed.
backend/data/models/

# SQLite
aml_agent.db

# Agent/editor tooling (skills installed via `npx skills add`) - local developer setup, not
# part of the deliverable.
.agents/
.claude/
skills-lock.json
